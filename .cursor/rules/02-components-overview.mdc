---
description:
globs:
alwaysApply: false
---
# PC组件库使用指南

本文档介绍了 `packages/pc/src/components` 目录下可用的通用组件及其使用方法。在开发过程中，优先使用这些组件，其次再考虑使用 Ant Design Vue 组件。

## 可用组件列表

- `Table`: 表格组件，封装自 Ant Design Vue 的 Table 组件
- `Form`: 表单组件，快速生成表单
- `Modal`: 对话框组件，支持自动销毁和插槽透传
- `Card`: 卡片组件，提供统一的卡片样式
- `Upload`: 上传组件，支持图片上传、预览和自定义验证
- `Search`: 搜索组件，提供展开/收起、重置、查询等功能
- `Page`: 页面容器组件，提供统一的页面容器样式
- `DynamicTooltip`: 动态提示组件
- `deleteModal`: 删除确认弹窗
- `secretKey`: 密钥管理组件
- `errPage`: 错误页面组件

## 编码规范

### Vue 规范

1. 使用 Composition API 的 `<script setup>` 语法
2. 使用 `ref` 和 `reactive` 声明响应式状态
3. 使用 `defineProps` 和 `defineEmits` 定义组件属性和事件

### TypeScript 规范

1. 为变量、函数参数和返回值添加适当的类型注解
2. 使用接口或类型别名定义复杂数据结构
3. 优先使用项目中已定义的类型和枚举

### CSS 规范

1. 优先使用UnoCSS提供的原子化类
2. 组件样式使用 `scoped` 或 `module` 防止样式污染
3. 复杂样式可以使用Less预处理器

## 使用建议

1. 开发新页面时，优先使用这些公共组件，定义存在于packages/pc/auto-import中
2. 对于表格和表单，建议将配置提取到单独的配置文件中
3. 如果现有组件无法满足需求，再考虑使用 Ant Design Vue 原生组件
4. 需要扩展组件功能时，请保持与原有设计风格一致
